import 'package:flutter/material.dart';

class MyAppColors {
  static LinearGradient cardTileColor =
      const LinearGradient(colors: [Color(0xff5936B4), Color(0xff362A84)]);

  static SweepGradient searchbarcolor = const SweepGradient(
    center: Alignment.topLeft,
    colors: [
      Color(0xff2E335A),
      Color(0xff1C1B33),
    ],
  );
}
